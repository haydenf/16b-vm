const MOVE_LIT_R1 = 0x10;
const MOVE_LIT_R2 = 0x11;
const ADD_REG_REG = 0x12;

//naming scheme is like this because we might have different move instructions

module.exports = {
	MOVE_LIT_R1,
	MOVE_LIT_R2,
	ADD_REG_REG,
};
